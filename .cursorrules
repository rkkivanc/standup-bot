This repository uses scoped rule files in `.cursor/rules/`:

- `frontend-standup-bot.mdc`
- `backend-standup-bot.mdc`
- `privacy-standup-bot.mdc`

Keep updates in those files to preserve targeted rule application.